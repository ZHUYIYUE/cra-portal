# Legacy Flask Archive

This folder contains the old Flask/JSON-file implementation that is no longer used by the current CRA Portal deployment.

Current production code is the static app at the repository root:

- `index.html`
- `static/`
- `supabase/`

These legacy files are kept only as historical reference. Do not deploy them to GitHub Pages and do not use them as the source of truth for new features.